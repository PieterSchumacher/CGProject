#include "Triangle.h"
#include "Ray.h"
#include "../Eigen/Geometry"
#include <iostream>

bool Triangle::intersect(const Ray &ray, double min_t, double &smallest_t_so_far, Vector3d &n) const {
    return false;
}


